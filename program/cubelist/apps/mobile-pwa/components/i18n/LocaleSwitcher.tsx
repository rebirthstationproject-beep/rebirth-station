/**
 * @deprecated 2026-05-22 — `@/components/layout/LocaleSwitcher` 로 통합되었습니다.
 *
 * 본 모듈은 sub-tree에서 import 잔존 시 빌드를 깨뜨리지 않기 위한 shim 입니다.
 * 새 코드에서는 `components/layout/LocaleSwitcher` 의 `LocaleSwitcher` 를 사용하세요.
 *
 * 사용 형태:
 *   import { LocaleSwitcher } from '@/components/layout/LocaleSwitcher';
 *   <LocaleSwitcher size="compact" />
 *
 * 다음 릴리스 (Stage 2 정리 시점) 에서 본 파일은 삭제될 예정입니다.
 */

export { LocaleSwitcher } from '@/components/layout/LocaleSwitcher';
