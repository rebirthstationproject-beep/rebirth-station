# 환경변수 (절대 commit 금지)
.env
.env.local
.env.*.local

# 의존성·빌드 산출 (현재 React UMD = node_modules 없음, 향후 SSR 마이그레이션 대비)
node_modules/
.next/
out/
dist/
build/

# Rust / Tauri 빌드 산출물
target/
*.pdb
*.rlib
src-tauri/target/
src-tauri/gen/schemas/

# TypeScript 부산물 (frontend src/ 안 .js 생성 방지 — tsc noEmit:true 와 이중 안전망)
**/frontend/src/**/*.js
**/frontend/src/**/*.js.map
*.tsbuildinfo

# OS·에디터
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~

# 로그
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 임시
.tmp/
.cache/
*.tmp

# uploads/ 안 사용자 업로드 (Stage 2~)
# uploads/  # 필요 시 주석 해제
